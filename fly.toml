app = "zeppelinker"
primary_region = "ams"
kill_signal = "SIGINT"
kill_timeout = "5s"

[build]

[env]
  STATE_DB_PATH = "/data/state.db"

[[mounts]]
  source = "zeppelinker_data"
  destination = "/data"
